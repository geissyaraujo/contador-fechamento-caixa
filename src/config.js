// Configuração de URL base da API.
// Altere esta constante para apontar para o ambiente desejado (dev, homologação, produção).
// Exemplo: 'https://api.seudominio.com'
const API_BASE_URL = window.API_BASE_URL || 'http://localhost:5001';

